import 'package:flutter/material.dart';

final background = LinearGradient(
  begin: Alignment.topLeft,    // Define the starting point of the gradient
  end: Alignment.bottomRight,  // Define the ending point of the gradient
  colors: [
   Color(0xff4093CE),   // The list of colors to transition between
     Color(0xff9BCEF3),
    
  ],
);


var primary = Color(0xff4093CE);

var buttonColor = Color(0xff1BBFB8);
